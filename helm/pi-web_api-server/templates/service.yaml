apiVersion: v1
kind: Service
metadata:
  name: {{ include "pi-web_api-server.fullname" . }}
  labels:
    {{- include "pi-web_api-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 30080
  selector:
    {{- include "pi-web_api-server.selectorLabels" . | nindent 4 }}
