apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pi-web_frontend.fullname" . }}-test-connection"
  labels:
    {{- include "pi-web_frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pi-web_frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
