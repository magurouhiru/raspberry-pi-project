# health
GET         /health           controllers.HealthController.health()
GET         /ready            controllers.HealthController.ready()

# API
GET         /api/hello        controllers.HelloController.get()
POST        /api/hello        controllers.HelloController.echo()
