# health
GET         /health                                 controllers.HealthController.health()
GET         /ready                                  controllers.HealthController.ready()

# batch
GET         /batch/device-all                       controllers.BatchController.deviceAll()

# API
->          /api api.Routes

# public
GET         /                                       controllers.HomeController.index(path="/public/frontend/browser", indexFile = "index.html")
GET         /*file                                  controllers.HomeController.serveOrFallback(path="/public/frontend/browser", file)
